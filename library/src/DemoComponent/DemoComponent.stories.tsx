import React from 'react';
import { DemoComponent } from './DemoComponent';

export default {
  title: 'DemoComponent',
};

export const Primary = () => <DemoComponent />;

export const Secondary = () => <DemoComponent />;
